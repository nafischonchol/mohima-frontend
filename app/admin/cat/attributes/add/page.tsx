import AddAttributeClient from "./AddAttributeClient";

export const metadata = {
  title: "Add Attribute - POS Admin",
  description: "Create a new product attribute with predefined option values and swatches.",
};

export default function AddAttributePage() {
  return <AddAttributeClient />;
}
